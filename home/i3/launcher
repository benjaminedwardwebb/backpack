# i3/launcher
# i3 configuration file that manages the program launcher.
#
# I use dmenu, the i3 default.
#
# vim: filetype=i3config

# Start dmenu-rs (a program launcher) using the modifier key and a forward
# slash, like searching in vim. Style parameters are also passed.
# See: https://github.com/Shizcow/dmenu-rs
# See: https://tools.suckless.org/dmenu/
bindsym $mod+slash exec dmenu_run --lines 0 --font 'Fira Code Nerd Font-20' --nb $background --nf $foreground --sb $emphasizedBackground --sf $accentColor

# Equivalent command for the original dmenu written in C.
#bindsym $mod+slash exec dmenu_run -p "" -l 0 -fn 'Fira Code Nerd Font-20' -nb $background -nf $foreground -sb $emphasizedBackground -sf $accentColor
